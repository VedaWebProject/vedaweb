All *.xml files in this directory will be used for data import.
This is only relevant for development. An instance running in production from a packaged JAR needs an external "tei" directory containing the xml files inside the same directory as the JAR.
